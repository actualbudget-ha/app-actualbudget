[project]
name = "app-actualbudget"
version = "0.1.0"
description = "Development tooling for the app-actualbudget Home Assistant app repository."
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
  "yamllint>=1.37.1",
]

[tool.uv]
package = false
